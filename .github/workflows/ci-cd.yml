name: ci-cd-pipeline

on:
  push:
    branches:
      - 'master'
      - 'dev'
      - 'qa'
      - 'prod'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build
        run: |
          # Add build commands here
  
